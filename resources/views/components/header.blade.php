<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UPT Komputer</title>

    <style>
        .card-header {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            /* Align items to the start of the container */
            gap: 10px;
            /* Space between elements */
            padding: 10px;
            /* Optional: padding around the card header */
        }

        .card-header form {
            display: flex;
            align-items: center;
            gap: 5px;
            /* Space between the search button and input */
        }

        .search-input {
            width: 200px;
            /* Adjust width as needed */
            font-size: 0.875rem;
            /* Smaller font size for the input */
            padding: 0.25rem 0.5rem;
            /* Smaller padding */
        }

        .btn {
            margin-left: 5px;
            /* Margin between buttons */
        }
    </style>




    <x-link></x-link>
</head>
